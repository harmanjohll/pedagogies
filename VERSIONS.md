# Co-Cher Version Lineage

The canonical URL always serves the **latest** version:
`https://harmanjohll.github.io/pedagogies/` → redirects to `app/cocher.html`.

| Version | Location | Live URL | Storage namespace | Status |
|---------|----------|----------|-------------------|--------|
| v1 | `archive/cocher-v1.html` (single-file: Lesson Planner chat + Spatial Designer tabs) | `/pedagogies/archive/cocher-v1.html` (old `/pedagogies/cocher.html` bookmarks redirect here) | `cocher_api_key`, `cocher_model`, `darkMode`, `spatialPlannerCustomPresets` | Archived |
| v2 | `archive/v2/` (frozen snapshot of the modular app as of Jul 2026) | `/pedagogies/archive/v2/cocher.html` | `cocher_v2_*` (renamed at freeze time so the archive can never touch live data) | Archived |
| v3 | `archive/v3/` (frozen snapshot as of Jul 2026 — v3.1 feature set) | `/pedagogies/archive/v3/cocher.html` | `cocher_v3_*` (renamed at freeze time) | Archived |
| v4 | `archive/v4/` (frozen snapshot as of Jul 2026 — "Staffroom Desk" design + learner portraits, growth engine, department packs, as originally shipped) | `/pedagogies/archive/v4/cocher.html` | `cocher_v4_*` (renamed at freeze time) | Archived |
| v4.1 | `archive/v4.1/` (frozen snapshot as of Jul 2026 — domain-accuracy audit sweep: assessment/spatial/simulation fixes, admin workflow bug fix, Sec 1/2 CCE lessons, CCA exemplars, demo events, as originally shipped) | `/pedagogies/archive/v4.1/cocher.html` | `cocher_v4_1_*` (renamed at freeze time) | Archived |
| v5.1 | `archive/v5.1/` (frozen snapshot as of Jul 2026 — inviting/engaging generation: living lesson journey, workflow modes, visual teacher identity, configurable tracking schemas, bulk student upload, remarks, My References; incl. Sem 2 timetable refresh, as originally shipped) | `/pedagogies/archive/v5.1/cocher.html` | `cocher_v5_1_*` (renamed at freeze time) | Archived |
| v6 | `archive/v6/` (frozen snapshot as of Jul 2026 — "Showtime": run-of-show staging, first-class scenes, students on the canvas, Class Screen projector view, simulation one-stop, as originally shipped) | `/pedagogies/archive/v6/cocher.html` | `cocher_v6_*` (renamed at freeze time) | Archived |
| v6.1 | `archive/v6.1/` (frozen snapshot as of Jul 2026 — "Wieldable": cockpit + journey bar, named seats, TLDR+expand plans, pedagogy frameworks, sim depth, as originally shipped) | `/pedagogies/archive/v6.1/cocher.html` | `cocher_v6_1_*` (renamed at freeze time) | Archived |
| v6.2 | `archive/v6.2/` (frozen snapshot as of Jul 2026 — "Effortless": auto-staging, materials (HTML decks + TTS audio), CCE through the planner, flow-audit repairs, Present-mode fit, as originally shipped) | `/pedagogies/archive/v6.2/cocher.html` | `cocher_v6_2_*` (renamed at freeze time) | Archived |
| v7 | `archive/v7/` (frozen snapshot as of Jul 2026 — "Anywhere": mobile daily loop, offline/PWA service worker, anticipatory next-lesson home, growth loop + practice story, voice input, milestone moments, as originally shipped) | `/pedagogies/archive/v7/cocher.html` | `cocher_v7_*` (renamed at freeze time) | Archived |
| v8.4 | `app/` (active development — "Refine + Rearrange + Teach + Present + Showcase + Attach + Audience + Live": v7.1's findable tools / work-that-saves / relief-admin fixes / accessibility, the Find-a-Teacher availability checker with a date picker, and a big interactive live seating chart in Present (free-drag students + move furniture, Save/Reset; v7.4 enlarged the seat map and made "Open" carry the room + seated students into the designer); v7.5 restructures the lesson plan around the **Singapore Teaching Practice** — per-segment Teaching Area + Teaching Action pickers (CCE-style, with an "Other" escape hatch), an STP-structured Run of Show cockpit with a Details button, the action's student-facing framing carried through to Present, an STP-aware AI stager, and a non-destructive "Map to STP" retrofit for existing lessons — all additive, every AI tool retained; v7.6 makes students draggable in the Spatial Designer (drag any pill + Save) and rebuilds the slide-deck output into professional, multi-layout slides (title / section / statement / quote / columns / chart / exit) with inline SVG charts, diagrams and icons, smooth transitions and a progress bar, plus AI-filled diagrams/images where they aid understanding; v7.7 seeds three fully-staged **showcase lessons** (Chemistry — Acids/Bases/Salts, Geography — Volcanoes/Earthquakes, and a CCE Cyber Wellness lesson), each with an STP-tagged run of show, a class seated in discussion pods, and a pre-built professional deck attached; v7.8 lets teachers **attach images and PDFs** to the Lesson Planner chat (drag-drop or "Add file" — worksheets, textbook pages, diagrams, rubrics, photos of student work) which Gemini reads natively (images downscaled, oversized PDFs falling back to extracted text; base64 stripped before persistence so the storage quota is safe), adds a **Teaching Focus Areas** manager in Settings (toggle the presets, add/remove your own) whose areas now actively shape and upskill lesson design, and tidies the lesson lifecycle stepper onto a single row with clearer labels — Design · Prepare · Rehearse · Teach · Reflect; v7.9 adds an **audience-facing presentation** — an Audience mode in Present (assembly / open-house: hides class-only seating & groups, ends on a "take your card" screen) and a **Takehome Card** generator (a one-page, BTY-styled card built from any lesson — printable several-per-A4, downloadable, and shareable as a `#/card/…` digital link that needs no backend), plus a large **reliability + mobile sweep** fixing every issue from the v7.8 audit — attachment MIME failures (gif/svg now re-encode to JPEG), autosave data-loss/duplicate-lesson races, polluted lesson titles, deck YouTube-URL parsing and exit behaviour, exemplar-deck orphaning, and the phone layout of the composer toolbar, deck controls and Present control bars; v8.0 adds **Live Sessions** — a "Live" option in the deck builder that runs a deck as a two-device workshop (teacher presents on the projector, students join on their phones with a room code and follow + submit in lockstep: predict-&-reveal quizzes with live vote distributions, confidence polls, an anonymous word-wall, and a personalised "command card" each phone builds from its own answers), synced client-side over MQTT-over-WebSockets on a public broker (HiveMQ by default) with a same-device BroadcastChannel fallback; the phone surface is one generic hosted page (`live/join.html`) that renders whatever the presenter broadcasts, nothing is stored server-side, and a standalone Acids/Bases/Salts demo lives at `live/acids-live.html`); v8.1 pulls Live together for everyday use — the presenter's join screen now shows a **scan-to-join QR code** (encoding the join URL + room; the room code + link remain as a fallback; a vendored MIT `live/qrcode.min.js` renders it offline-safe), and the three **showcase lessons ship Live-ready**: their attached decks store the slide MODEL (not just compiled HTML) alongside predict-&-reveal quizzes / live polls / per-lesson word-wall prompts, so a **one-tap "Go Live"** on any demo deck (in the lesson detail + planner resource rows) runs the whole two-device workshop with no setup — powered by a shared `openLiveSession` launcher so the deck-builder and demo-lesson paths never drift; plus an urgent in-class fix pass: decks now open in an **in-app viewer** (`#/deck/:id`) instead of a blob: tab, which is what let YouTube embeds fail with player error 153 (no referrer); the planner↔designer links are reliable — "Open" always loads the linked room and paints the segment's **seated student names** (deriving legacy seat maps), the lesson page's "Open in Designer" carries its payload, and the designer's "Use in Lesson Planner" no longer drops the open lesson; the sim overlay truly contains practicals (titration et al) — columns stack below 1024px on any pointer device, overflow scrolls instead of clipping, and all 28 sims defer their Google-Fonts load so first paint isn't blocked on school networks; and Lesson Rehearsal's voice input uses the shared hardened dictation helper (no more stuck "Listening…" or double-start errors); v8.2 brings Live fully **inside Co-Cher**: the runtime is a real ES module (`utils/live-engine.js`, lv-scoped styles, MQTT + BroadcastChannel bus with proper teardown) mounted by two app routes — `#/live`, the presenter, launched same-tab fullscreen from a **"▶ Go Live" button in Present** (and from decks via the shared launcher; the deck rides a sessionStorage stash so a mid-lesson refresh keeps the SAME room), and `#/join(/:room)`, the student surface the projector QR now encodes — booted before the login/onboarding flow so students need no account; `mqtt.js`/`qrcode.js` lazy-load only on these routes, `live/join.html` is now just a legacy redirect into `#/join`, and the wire protocol is unchanged so the standalone `live/acids-live.html` demo still interoperates; v8.3 makes the LabSim practicals **fit any screen** — the sim overlay scale-to-fits the whole practical when its panels' fixed minimums exceed the window (no more clipped data panels on projectors) and gains a ⛶ Fullscreen button; voice input reaches **parity everywhere** (a route change aborts any live dictation centrally in the router, and planner/reflection/class-note/rehearsal mics all share one honest error voice: mic blocked / needs internet / no mic found); Live's session stash is cleared on clean exit so back-button can never resurrect a finished room, and the projector's typable fallback link now really resolves (`…/pedagogies/#/join` — the root redirect preserves the hash); v8.4 makes sim containment **universal** — the scale-to-fit engine covers all ~28 sims via an explicit fit-root registry (v8.3 reached only the 3 `.practical-layout` practicals), the injected base CSS guarantees **scrolling as the always-available escape hatch** (the old `overflow-x:hidden` trap is gone; classic visible scrollbars; runtime unclip classes when even the 0.55 zoom floor can't fit), the sim window gains **manual − / % / + / Fit zoom controls** and a breathing gutter when dragged off-full, the floating notebook FAB/panel and tutorial tooltips are clamped so they never bury or escape a small window, and the Acids + Earthquakes showcase lessons **embed their simulation in the lesson flow** — segments carry `simId`, Present and the lesson page show a Launch-simulation button at exactly the right moment, and the sim overlay opens on top (closing returns to the same screen) via the new `launchSimById` export) | `/pedagogies/` (via `index.html`) | `cocher_*` (incl. `cocher_app_data`) | **Current** |

## Notes

- All versions share one browser origin (GitHub Pages), so localStorage/sessionStorage keys
  are shared between them. That is why the v2 archive's keys were renamed to `cocher_v2_*`
  at freeze time — opening the archive will never read or corrupt current data. The archive
  therefore starts "empty" (fresh login/API key) by design.
- The v2 archive still reports usage events to the same analytics Google Sheet as the live app.
- `backup.html` in the repo root is a backup of the **Spatial Pedagogy Planner v6.0**, not Co-Cher.
  The `spatialplanner*.html` files follow the same convention as this table: the unversioned
  file is the latest, versioned files are archives.
- Sign-in in all versions is a client-side roster check (timetable CSV), not real authentication.

## Archiving a future version (e.g. freezing v5.1 before the next release)

1. `cp -r app archive/vN` (drop `labsim.zip`). If the current working tree already contains work
   for the NEXT version, freeze from the shipped commit instead so the archive reflects what was
   actually released: `git archive <shipped-vN-commit> app | tar -x -C archive/vN`.
2. In `archive/vN/` only: `sed -i "s/'cocher_/'cocher_vN_/g"` across `*.js`/`*.html`
   (storage keys only — do NOT rename the `_cocher_lesson` share-format marker; note the dot in a
   version like `v4.1` becomes an underscore in the namespace: `cocher_v4_1_`).
3. Add the archived banner to `archive/vN/cocher.html` (copy from the previous archive's banner).
4. Update the table above, and in `app/js/version.js` bump `APP_VERSION` and prepend the newly
   archived version to `PREVIOUS_VERSIONS`.
5. **Service worker (since v7):** the live app registers `app/sw.js` (scope `app/`) with a
   version-keyed cache (`cocher-<APP_VERSION>`) and network-first HTML, so a fresh deploy never
   serves stale. When freezing, the archived copy under `archive/vN/` is OUTSIDE the `app/` scope,
   so it is never controlled by the live SW. If a frozen snapshot itself contains the SW
   registration + `sw.js`, that's harmless (it would register a separate, isolated scope under
   `archive/vN/`) — but prefer to delete `archive/vN/sw.js` and the registration block from the
   archived `cocher.html` so archives stay purely static. Bump the `VERSION` constant in
   `app/sw.js` to match `APP_VERSION` on every release so the cache busts.
